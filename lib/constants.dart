import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFF5C00);
const secondaryColor = Color(0xff9B9B9B);

// reset password
const tertiaryColor = Color(0xFF818181);

// For the Notifications page.
const grayColor = Color(0xff494949);

// home page and visit history
const primaryTextColor = Color(0xff0E0E0E);
const secondaryTextColor = Color(0xff494949);
